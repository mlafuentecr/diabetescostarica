import React from "react"
import LayoutIndex from "../components/layoutIndex"
import SEO from "../components/seo"

const IndexPage = (props) => (

  < LayoutIndex >
    <SEO title={'索引首頁'}  lang={'zh'} defer={false}/>
  </LayoutIndex >
)

export default IndexPage
